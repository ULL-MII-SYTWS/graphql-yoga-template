const posts = require('./postData');
//Resolvers - This are the set of the function defined to get the desired output for the given API
const resolvers = {
    Query: {
      // Write the query resolver function here
    },

    Mutation: {
      // Write the mutation resolver function here
    },

    Subscription: {
      // Write the subscription resolver function here
    },
}
module.exports = resolvers;
